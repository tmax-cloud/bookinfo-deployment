apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ template "core.app" . }}-vs
  labels:
{{ include "core.labels" . | indent 4 }}
spec:
  hosts:
    - {{ .Values.externalDomain }}
  gateways:
    - {{ .Values.ingressGateway }}
  http:
    - name: {{ template "core.app" . }}-route1
      match:
        - uri:
            prefix: /api/books
      route:
        - destination:
            host: {{ template "core.app" . }}-svc
            subset: stable
            port:
              number: 8080
          weight: 100
        - destination:
            host: {{ template "core.app" . }}-svc
            subset: canary
            port:
              number: 8080
          weight: 0